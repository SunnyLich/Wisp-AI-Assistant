# Manual QA manifest for an addon that fails during import.
[addon]
id = "phase4-bad-import"
name = "Phase 4 Bad Import"
version = "1.0.0"
description = "Manual QA addon that intentionally fails during import."
entry = "__init__.py"
api_version = "1"

[permissions]
query = "modify"

