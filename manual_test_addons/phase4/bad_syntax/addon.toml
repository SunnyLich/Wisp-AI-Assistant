# Manual QA manifest for an addon with intentionally invalid Python syntax.
[addon]
id = "phase4-bad-syntax"
name = "Phase 4 Bad Syntax"
version = "1.0.0"
description = "Manual QA addon that intentionally has a syntax error."
entry = "__init__.py"
api_version = "1"

[permissions]
query = "modify"

