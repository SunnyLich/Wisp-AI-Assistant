# Manual QA manifest for an addon that force-exits its host process.
[addon]
id = "phase4-host-exit"
name = "Phase 4 Host Exit"
version = "1.0.0"
description = "Manual QA addon that force-exits its host during a query hook."
entry = "__init__.py"
api_version = "1"

[permissions]
query = "modify"

