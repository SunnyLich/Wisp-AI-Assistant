.env
private/
doll/

# Memory (vector store + fallback JSON — contains personal user data)
memory/chroma/
memory/facts_fallback.json
# keep memory/.gitkeep so the folder is tracked

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/

# OS
.DS_Store
Thumbs.db
